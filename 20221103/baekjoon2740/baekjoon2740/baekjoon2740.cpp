#define collection cin.tie(NULL); ios_base::sync_with_stdio(false);
#include <iostream>
#include <vector>

using namespace std;
int x,y,m,n;
void Init();
vector<int> _slot;
vector<int> _slot2;
int main()
{
	Init();
}
void Solution()
{
	for (int i = 0; i < x; i++)
	{
		_slot[i]*[]
	}
}

void Init()
{
	collection;

	cin >> x >> y;
	for (int i = 0; i < x; i++)
	{
		for (int j = 0; j < y; j++)
		{
			int datas;
			cin >> datas;
			_slot.push_back(datas);
		}
	}	

	cin >> m >> n;
	for (int i = 0; i < m; i++)
	{
		for (int j = 0; j < n; j++)
		{
			int datas;
			cin >> datas;
			_slot2.push_back(datas);
		}
	}

	Solution();
}