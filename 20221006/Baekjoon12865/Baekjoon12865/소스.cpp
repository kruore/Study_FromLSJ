#include <iostream>
#include <vector>

using namespace std;

int max_weight;
int max_value;
pair<int, int> scs;
vector<pair<int, int>> data;

int main()
{
	cin >> max_weight >> max_value;
	for (int i = 0; i < max_weight; i++)
	{
		cin >> scs.first >> scs.second;

	}
}